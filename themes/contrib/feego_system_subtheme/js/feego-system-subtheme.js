/**
 * @file
 * feego_system_subtheme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.feegoSystemSubtheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
